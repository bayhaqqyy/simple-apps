services:
  apps:
    build: ./apps
    ports:
      - "80:3000"
    restart: always
    container_name: simple-apps
    environment:
      - DB_HOST
      - DB_USER
      - DB_PASS
      - DB_NAME
      - APP_PORT